name: privacy_shield
description: Local-first Android privacy firewall for camera, microphone, location and tracker DNS filtering.
publish_to: 'none'
version: 1.1.0+2

environment:
  sdk: '>=3.13.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  sqflite: ^2.4.3
  path: ^1.9.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
