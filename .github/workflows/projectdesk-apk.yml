name: ProjectDesk Android APK

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  parked:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Workflow temporarily parked while ProjectDesk v1.1 voice patch is uploaded."
